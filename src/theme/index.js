import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette:{
        background:{
            default:'#2c3e50'
        },
        primary:{
            main: '#2c3e50',
        },
        secondary:{
            main:'#f03434',
        }
    }
});

export default theme;